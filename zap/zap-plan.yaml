env:
  contexts:
  - name: "juice-shop"
    urls:
    - "http://localhost:3000"
    includePaths: []
    # Tell ZAP to NEVER scan or click on the logout path to avoid losing the session
    excludePaths:
    - "http://localhost:3000/rest/user/logout"

    # Define Authentication Strategy (Juice Shop uses JSON authentication)
    authentication:
      method: "json"
      parameters:
        loginPageUrl: "http://localhost:3000/#/login"
        loginRequestUri: "http://localhost:3000/rest/user/login"
        loginRequestBody: '{"email":"{%username%}","password":"{%password%}"}'
      # How ZAP verifies if the login was successful by looking at the response body
      verification:
        method: "response"
        loggedOutRegex: "401 Unauthorized"
        loggedInRegex: ".*token.*"

    # Define the Users
    users:
    - name: "admin-user"
      credentials:
        username: "admin@juice-sh.op"
        password: "admin123"

jobs:
# Step 1: AJAX spider — launches a real browser to crawl the Angular SPA
- type: spiderAjax
  parameters:
    context: "juice-shop"
    user: "admin-user"
    url: "http://localhost:3000"
    maxDuration: 10
    browserId: firefox-headless

# Step 2: Actively attack all discovered endpoints
- type: activeScan
  parameters:
    context: "juice-shop"
    user: "admin-user"
    maxScanDurationInMins: 60
  policyDefinition:
    defaultStrength: High
    defaultThreshold: Low

# Step 4: Generate HTML report
- type: report
  parameters:
    template: traditional-html
    reportDir: /zap/wrk
    reportFile: zap-report.html
    reportTitle: "ZAP Nightly Full Scan Report"
